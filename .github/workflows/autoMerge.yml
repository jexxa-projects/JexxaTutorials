name: Merge me!

on:
  workflow_run:
    types:
      - completed
    workflows:
      - 'Maven Test Build'

jobs:
  merge-me:
    name: Merge me!

    runs-on: ubuntu-latest

    steps:
      - name: Merge me!
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        uses: ridedott/merge-me-action@fffaef0380e407b7cab584df2e68e6523b32fc74
        with:
          GITHUB_TOKEN: ${{ secrets.MERGE_ME_SECRET }}
          PRESET: DEPENDABOT_MINOR
