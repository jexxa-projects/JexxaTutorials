name: Merge me!

on:
  workflow_run:
    types:
      - completed
    workflows:
      - 'Maven Test Build'

jobs:
  merge-me:
    name: Merge me!

    runs-on: ubuntu-latest

    steps:
      - name: Merge me!
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        uses: ridedott/merge-me-action@17f90aac72fc476331601f9cf2c09087c6872038
        with:
          GITHUB_TOKEN: ${{ secrets.MERGE_ME_SECRET }}
          PRESET: DEPENDABOT_MINOR
